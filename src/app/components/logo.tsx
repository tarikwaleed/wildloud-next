import React from 'react';

type Props = {};

const Logo = (props: Props) => {
    return (
        <div>
            <img src="/images/logo.png" alt="App Logo" />
        </div>
    );
};

export default Logo;
